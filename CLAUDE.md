# Claude Notes

## Development Environment

- Do not try to build the site - the user has a dev server running (`npm run dev`) that handles live reloading
- Node/npm may not be available in the shell environment, but that's fine since changes can be verified via the running dev server
